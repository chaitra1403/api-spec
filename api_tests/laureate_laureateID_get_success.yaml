# Test generated by RoostGPT for test csv-testing-artillery using AI Type Open AI and AI Model gpt-4


# Test generated for /laureate/{laureateID}_get for http method type GET in artillery framework

# RoostTestHash=100d61e03c


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  payload:
    path: "laureate_laureateID_get_success.csv"
    fields:
      - "laureateID"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "API requests scenario"
    flow:
      # Get request for /2.1/laureate/{laureateID} endpoint
      - get:
          url: "/2.1/laureate/{{ laureateID }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasHeader: "Content-Type"
            - contentType: "application/json"
            - hasProperty: "laureateID"
          think: 1
