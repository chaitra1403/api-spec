# Test generated by RoostGPT for test csv-testing-artillery using AI Type Open AI and AI Model gpt-4


# Test generated for /nobelPrizes_get for http method type GET in artillery framework

# RoostTestHash=ed4795f9f6


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 20
  plugins:
    expect: {}
  payload:
    path: "./nobelPrizes_get_others.csv"
    fields:
      - "offset"
      - "limit"
      - "nobelPrizeCategory"

scenarios:
  - name: "Test scenario for non-successful API requests"
    flow:
      - log: "Sending GET request with correct URL but incorrect query parameters"
        get:
          url: "/2.1/nobelPrizes?offset={{ offset }}&limit={{ limit }}&nobelPrizeCategory={{ nobelPrizeCategory }}"
          expect:
            - statusCode: 400
          think: 1

      - log: "Sending GET request with incorrect URL"
        get:
          url: "/2.1/nobelPrizes/badentity"
          expect:
            - statusCode: 404
          think: 1

      - log: "Sending GET request with missing authentication"
        get:
          url: "/2.1/nobelPrizes"
          expect:
            - statusCode: 401
          think: 1
