# Test generated by RoostGPT for test csv-testing-artillery using AI Type Open AI and AI Model gpt-4


# Test generated for /nobelPrizes_get for http method type GET in artillery framework

# RoostTestHash=ed4795f9f6


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 5
  payload:
    path: "nobelPrizes_get_success.csv"
    fields:
      - "offset"
      - "limit"
      - "sort"
      - "nobelPrizeYear"
      - "yearTo"
      - "nobelPrizeCategory"
      - "format"
      - "csvLang"
    skipHeader: true
  plugins:
    expect: {}

scenarios:
  - name: "Testing Nobel Prizes API"
    flow:
      - get:
          url: "/2.1/nobelPrizes?offset={{ offset }}&limit={{ limit }}&sort={{ sort }}&nobelPrizeYear={{ nobelPrizeYear }}&yearTo={{ yearTo }}&nobelPrizeCategory={{ nobelPrizeCategory }}&format={{ format }}&csvLang={{ csvLang }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - hasProperty: "nobelPrizes"
            - hasProperty: "nobelPrizes[0].awardYear"
            - hasProperty: "nobelPrizes[0].category"
            - hasProperty: "nobelPrizes[0].categoryFullName"
            - hasProperty: "nobelPrizes[0].dateAwarded"
            - hasProperty: "nobelPrizes[0].prizeAmount"
            - hasProperty: "nobelPrizes[0].prizeAmountAdjusted"
            - hasProperty: "nobelPrizes[0].topMotivation"
            - hasProperty: "nobelPrizes[0].laureates"
            - hasProperty: "meta"
            - hasProperty: "links"
          think: 1
