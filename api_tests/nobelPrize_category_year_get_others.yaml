# Test generated by RoostGPT for test csv-testing-artillery using AI Type Open AI and AI Model gpt-4


# Test generated for /nobelPrize/{category}/{year}_get for http method type GET in artillery framework

# RoostTestHash=ed71bdf663


config:
  target: "{{ $processEnvironment.API_HOST }}"
  phases:
    - duration: 60
      arrivalRate: 10
  plugins:
    expect: {}
  payload:
    path: "nobelPrize_category_year_get_others.csv"
    fields:
      - "category"
      - "year"
    skipHeader: true

scenarios:
  - name: "Non-success API requests"
    flow:
      # Invalid category request
      - log: "Sending GET request with invalid category"
        get:
          url: "/2.1/nobelPrize/{{ category }}/2021"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
        think: 1

      # Invalid year request
      - log: "Sending GET request with invalid year"
        get:
          url: "/2.1/nobelPrize/peace/{{ year }}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
        think: 1

      # Invalid URL request
      - log: "Sending GET request to invalid URL"
        get:
          url: "/2.1/nobelPrize/peace/2021/invalid"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
        expect:
          - statusCode: 404
        think: 1

      # Unauthorized request
      - log: "Sending GET request without authorization"
        get:
          url: "/2.1/nobelPrize/peace/2021"
        expect:
          - statusCode: 401
        think: 1
