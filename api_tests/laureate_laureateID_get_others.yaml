# Test generated by RoostGPT for test csv-testing-artillery using AI Type Open AI and AI Model gpt-4


# Test generated for /laureate/{laureateID}_get for http method type GET in artillery framework

# RoostTestHash=100d61e03c


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "laureate_laureateID_get_others.csv"
    fields:
      - laureateID
    skipHeader: true
  phases:
    - duration: 60
      arrivalRate: 20
  defaults:
    headers:
      Authorization: "Bearer {{$processEnvironment.AUTH_TOKEN}}"

scenarios:
  - name: "Non-Success API Responses"
    flow:
      # Test with an invalid laureateID
      - get:
          url: "/2.1/laureate/{{ laureateID }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - hasProperty: "message"
          afterResponse: "think"
      # Test with missing Authorization header
      - get:
          url: "/2.1/laureate/{{ laureateID }}"
          headers:
            Authorization: ""
          expect:
            - statusCode: 401
            - hasProperty: "error"
            - hasProperty: "message"
          afterResponse: "think"
      # Test with an incorrect API endpoint
      - get:
          url: "/2.1/nonexistent/{{ laureateID }}"
          expect:
            - statusCode: 404
            - hasProperty: "error"
            - hasProperty: "message"
          afterResponse: "think"
      # Test with a malformed laureateID
      - get:
          url: "/2.1/laureate/malformed"
          expect:
            - statusCode: 400
            - hasProperty: "error"
            - hasProperty: "message"
          afterResponse: "think"
