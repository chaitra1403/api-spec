# Test generated by RoostGPT for test csv-testing-artillery using AI Type Open AI and AI Model gpt-4


# Test generated for /nobelPrize/{category}/{year}_get for http method type GET in artillery framework

# RoostTestHash=ed71bdf663


config:
  target: "{{ $processEnvironment.API_HOST }}"
  plugins:
    expect: {}
  payload:
    path: "nobelPrize_category_year_get_success.csv"
    fields:
      - "category"
      - "year"
    skipHeader: true
  headers:
    Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"

scenarios:
  - name: "Nobel Prize API Test Scenario"
    flow:
      # Get request to fetch Nobel Prize details for a specific category and year
      - get:
          url: "/2.1/nobelPrize/{{category}}/{{year}}"
          headers:
            Authorization: "Bearer {{ $processEnvironment.AUTH_TOKEN }}"
          expect:
            - statusCode: 200
            - contentType: "application/json"
            - hasProperty: "nobelPrize"
            - hasProperty: "nobelPrize.awardYear"
            - hasProperty: "nobelPrize.category"
            - hasProperty: "nobelPrize.category.en"
            - hasProperty: "nobelPrize.category.se"
            - hasProperty: "nobelPrize.category.no"
            - hasProperty: "nobelPrize.categoryFullName"
            - hasProperty: "nobelPrize.dateAwarded"
            - hasProperty: "nobelPrize.prizeAmount"
            - hasProperty: "nobelPrize.prizeAmountAdjusted"
            - hasProperty: "nobelPrize.topMotivation"
            - hasProperty: "nobelPrize.laureates"
        think: 1 # Pause for a second before the next request
